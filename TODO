* Refactor key parsing and walking to other files if possible
* Write tests
* Split some functionality into seperate files to simplify main
* Input globbing (ie: Name:*Firefox* instead of _needing_ the exact name)
